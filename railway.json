{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
  },
  "buildCommand": "pip install -r requirements.txt",
  "pythonVersion": "3.11"
}